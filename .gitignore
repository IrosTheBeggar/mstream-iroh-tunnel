# Rust build output (rebuilt via cargo / cargo-ndk)
/target

# Binaries staged by the build-*.sh scripts. Consumers copy them from here or
# take a tagged release's assets; nothing under dist/ is committed.
/dist

# Interop test harness deps (reinstalled via `npm install`)
/interop/node_modules
/interop/package-lock.json
